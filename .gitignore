# Java
target/
*.class
# IDE
.idea/
*.iml
.vscode/
# Frontend
node_modules/
dist/
# Logs / env
*.log
application-local.yml
# Nacos 配置存档(含真实连接,不入库)
docs/nacos/
# 连真实库的测试 profile(含真实连接)
application-seed.yml
# Claude 本地配置
.claude/

# 网关托管的前端构建产物(由 build-all.ps1 生成,不入库;保留 .gitkeep)
auth-gateway/src/main/resources/static/*
!auth-gateway/src/main/resources/static/.gitkeep
