# M9:部署与前端托管 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 网关托管前端静态包(单端口出整站),后端统一 `/api` 前缀,生产 Nacos namespace/profile 参数化,产出一键构建脚本 + 部署文档,本机起两个 jar 即可访问整站。

**Architecture:** 后端经网关时统一 `/api/**`(StripPrefix 转 auth-service)。网关 WebFlux 托管 `dist` 静态资源;一个 `SpaWebFilter` 把非 `/api`、无扩展名的路径重写到 `/index.html`(history 回退)。`SaReactorFilter` 只拦 `/api/**`,判定时剥掉 `/api` 前缀,复用现有白名单与动态RBAC 映射(不改 Nacos 映射、不改 auth-service)。bootstrap 的 namespace/profile 可由启动参数覆盖。

**Tech Stack:** Spring Cloud Gateway (WebFlux) + Sa-Token + Nacos + Vue3 构建产物;构建用 JDK21(`build.ps1`)。

> 路径基准:仓库根 `auth-wjb/`,当前分支 `master`,工作区干净。前端命令 `npm`,后端走 `build.ps1`。

---

## 文件结构

```
auth-gateway/src/main/java/com/wjb/auth/gateway/config/
├── SaTokenGatewayConfig.java          # 修改:只拦 /api/**,剥前缀后匹配
└── SpaWebFilter.java                  # 创建:SPA history 回退(路径重写到 index.html)
auth-gateway/src/main/resources/static/.gitkeep   # 创建:占位(构建时填 dist)
frontend/vite.config.js                # 修改:/api 代理去掉 rewrite
docs/nacos/auth-gateway-dev.yml        # 修改(存档)+ Nacos 控制台同步:路由改 /api/**
auth-service/src/main/resources/bootstrap.yml      # 修改:namespace/profile 参数化
auth-gateway/src/main/resources/bootstrap.yml      # 修改:namespace/profile 参数化
build-all.ps1                          # 创建:前端构建→拷 static→mvn package
.gitignore                             # 修改:忽略 static 构建产物(保留 .gitkeep)
docs/DEPLOY.md                         # 创建:部署文档
```

---

## Task 1:网关鉴权改为只拦 /api 并剥前缀

**Files:**
- Modify: `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SaTokenGatewayConfig.java`

- [ ] **Step 1: 全文替换 SaTokenGatewayConfig**

把 `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SaTokenGatewayConfig.java` 全文替换为:
```java
package com.wjb.auth.gateway.config;

import cn.dev33.satoken.context.SaHolder;
import cn.dev33.satoken.exception.NotLoginException;
import cn.dev33.satoken.exception.NotPermissionException;
import cn.dev33.satoken.reactor.filter.SaReactorFilter;
import cn.dev33.satoken.stp.StpUtil;
import cn.dev33.satoken.util.SaFoxUtil;
import com.wjb.auth.gateway.security.ApiPermCache;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.HashMap;
import java.util.Map;

/**
 * 网关统一鉴权:只拦 /api/**,判定时剥掉 /api 前缀得到逻辑路径(/auth/.. /system/..),
 * 复用白名单 + 动态RBAC 映射。静态资源(非 /api)不被拦截,由 WebFlux 静态处理器/SpaWebFilter 处理。
 */
@Configuration
public class SaTokenGatewayConfig {

    /** 鉴权白名单(逻辑路径,无需登录) */
    private static final String[] WHITELIST = {
            "/auth/login", "/auth/login/**", "/auth/sms-code", "/auth/email-code",
            "/auth/oauth/**", "/auth/captcha", "/health", "/doc.html",
            "/webjars/**", "/v3/api-docs/**", "/swagger-ui/**", "/favicon.ico"
    };

    @Bean
    public SaReactorFilter saReactorFilter(ApiPermCache apiPermCache) {
        return new SaReactorFilter()
                .addInclude("/api/**")
                .setAuth(obj -> {
                    String path = SaHolder.getRequest().getRequestPath();
                    // 剥掉 /api 前缀得到逻辑路径
                    String logical = path.startsWith("/api") ? path.substring(4) : path;
                    if (logical.isEmpty()) {
                        logical = "/";
                    }
                    // 白名单放行
                    for (String w : WHITELIST) {
                        if (SaFoxUtil.isMatch(w, logical)) {
                            return;
                        }
                    }
                    // 登录态
                    StpUtil.checkLogin();
                    // 动态权限:命中映射才校验权限码
                    String method = SaHolder.getRequest().getMethod();
                    String perm = apiPermCache.requiredPerm(method, logical);
                    if (perm != null) {
                        StpUtil.checkPermission(perm);
                    }
                })
                .setError(e -> {
                    SaHolder.getResponse().setHeader("Content-Type", "application/json; charset=utf-8");
                    Map<String, Object> body = new HashMap<>();
                    if (e instanceof NotLoginException) {
                        body.put("code", 401);
                        body.put("msg", "登录已过期,请重新登录");
                    } else if (e instanceof NotPermissionException) {
                        body.put("code", 403);
                        body.put("msg", "无操作权限");
                    } else {
                        body.put("code", 500);
                        body.put("msg", e.getMessage());
                    }
                    body.put("data", null);
                    return body;
                });
    }
}
```

- [ ] **Step 2: 编译并提交**

Run(仓库根):
```powershell
.\build.ps1 -q -pl auth-gateway -am compile
```
Expected: BUILD SUCCESS。
```bash
git add auth-gateway/src/main/java/com/wjb/auth/gateway/config/SaTokenGatewayConfig.java
git commit -m "feat(m9): 网关鉴权只拦 /api 并剥前缀复用白名单/RBAC"
```

---

## Task 2:SPA history 回退过滤器

**Files:**
- Create: `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SpaWebFilter.java`

- [ ] **Step 1: 写 SpaWebFilter**

Create `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SpaWebFilter.java`:
```java
package com.wjb.auth.gateway.config;

import org.springframework.stereotype.Component;
import org.springframework.web.server.ServerWebExchange;
import org.springframework.web.server.WebFilter;
import org.springframework.web.server.WebFilterChain;
import reactor.core.publisher.Mono;

/**
 * SPA history 路由回退:非 /api、非根、且无扩展名(即不是静态资源文件)的路径,
 * 重写为 /index.html,交给 WebFlux 静态资源处理器返回,使前端 history 路由刷新不 404。
 * 静态资源(含扩展名,如 .js/.css/.png)与 /api 请求不受影响。
 */
@Component
public class SpaWebFilter implements WebFilter {

    @Override
    public Mono<Void> filter(ServerWebExchange exchange, WebFilterChain chain) {
        String path = exchange.getRequest().getURI().getPath();
        boolean isApi = path.startsWith("/api");
        boolean isRoot = path.equals("/");
        boolean looksLikeFile = path.contains(".");
        if (!isApi && !isRoot && !looksLikeFile) {
            return chain.filter(exchange.mutate()
                    .request(exchange.getRequest().mutate().path("/index.html").build())
                    .build());
        }
        return chain.filter(exchange);
    }
}
```

- [ ] **Step 2: 编译并提交**

Run(仓库根):
```powershell
.\build.ps1 -q -pl auth-gateway -am compile
```
Expected: BUILD SUCCESS。
```bash
git add auth-gateway/src/main/java/com/wjb/auth/gateway/config/SpaWebFilter.java
git commit -m "feat(m9): SPA history 回退过滤器(重写到 index.html)"
```

---

## Task 3:Nacos 网关路由改 /api/**

**Files:**
- Modify: `docs/nacos/auth-gateway-dev.yml`(存档,gitignore)+ Nacos 控制台同步

- [ ] **Step 1: 改存档文件**

将 `docs/nacos/auth-gateway-dev.yml` 全文替换为:
```yaml
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/**
          filters:
            - StripPrefix=1
```

- [ ] **Step 2: 同步到 Nacos**

浏览器开 `http://192.168.0.230:8848/nacos`(nacos/nacos)→ 命名空间 `dy_wjb`(id=test)→ 配置 `auth-gateway-dev.yml` → 编辑为 Step 1 内容 → 发布。

> 说明:此改动后,经网关访问后端必须带 `/api` 前缀;前端 axios 已是 `baseURL=/api`,dev 的 Vite 代理在 Task 4 调整为不去前缀。

- [ ] **Step 3: 提交(存档 gitignore,不入库;此步无代码提交,仅记录)**

无需 git 提交(`docs/nacos/` 已忽略)。

---

## Task 4:前端 Vite 代理去掉 rewrite

**Files:**
- Modify: `frontend/vite.config.js`

- [ ] **Step 1: 改代理**

将 `frontend/vite.config.js` 的 `proxy` 段从:
```js
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
```
改为(去掉 rewrite,让 `/api` 原样到网关,由网关 StripPrefix):
```js
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
```

- [ ] **Step 2: 构建验证并提交**

Run(`frontend/`):
```powershell
cd frontend; npm run build
```
Expected: 构建成功。
```bash
git add frontend/vite.config.js
git commit -m "feat(m9): Vite 代理 /api 不再 rewrite(网关 StripPrefix 统一)"
```

---

## Task 5:bootstrap namespace/profile 参数化

**Files:**
- Modify: `auth-service/src/main/resources/bootstrap.yml`
- Modify: `auth-gateway/src/main/resources/bootstrap.yml`

- [ ] **Step 1: auth-service bootstrap 参数化**

将 `auth-service/src/main/resources/bootstrap.yml` 里的 `spring.profiles.active` 与两处 `namespace` 改为可覆盖:
- `active: dev` → `active: ${SPRING_PROFILES_ACTIVE:dev}`
- discovery 与 config 各自的 `namespace: test` → `namespace: ${NACOS_NAMESPACE:test}`

- [ ] **Step 2: auth-gateway bootstrap 参数化**

将 `auth-gateway/src/main/resources/bootstrap.yml` 同样改:
- `active: dev` → `active: ${SPRING_PROFILES_ACTIVE:dev}`
- 两处 `namespace: test` → `namespace: ${NACOS_NAMESPACE:test}`

- [ ] **Step 3: 编译并提交**

Run(仓库根):
```powershell
.\build.ps1 -q -DskipTests package
```
Expected: BUILD SUCCESS(三模块)。
```bash
git add auth-service/src/main/resources/bootstrap.yml auth-gateway/src/main/resources/bootstrap.yml
git commit -m "feat(m9): Nacos namespace/profile 启动参数化(默认 test/dev)"
```

---

## Task 6:一键构建脚本 + static gitignore

**Files:**
- Create: `build-all.ps1`
- Create: `auth-gateway/src/main/resources/static/.gitkeep`
- Modify: `.gitignore`

- [ ] **Step 1: 占位 .gitkeep**

Create `auth-gateway/src/main/resources/static/.gitkeep`(空文件)。

- [ ] **Step 2: .gitignore 忽略 static 产物**

在 `.gitignore` 末尾追加:
```gitignore
# 网关托管的前端构建产物(由 build-all.ps1 生成,不入库;保留 .gitkeep)
auth-gateway/src/main/resources/static/*
!auth-gateway/src/main/resources/static/.gitkeep
```

- [ ] **Step 3: build-all.ps1**

Create `build-all.ps1`:
```powershell
# 一键构建:前端 → 拷入网关 static → 打包后端(含前端的网关 jar)
$ErrorActionPreference = "Stop"
$env:JAVA_HOME = "D:\Program Files\Java\jdk-21.0.10"
$root = $PSScriptRoot

Write-Host "[1/3] 构建前端..." -ForegroundColor Cyan
Push-Location "$root\frontend"
npm install --no-audit --no-fund
npm run build
Pop-Location

Write-Host "[2/3] 拷贝 dist 到网关 static..." -ForegroundColor Cyan
$static = "$root\auth-gateway\src\main\resources\static"
if (Test-Path $static) { Get-ChildItem $static -Exclude ".gitkeep" | Remove-Item -Recurse -Force }
New-Item -ItemType Directory -Force -Path $static | Out-Null
Copy-Item "$root\frontend\dist\*" -Destination $static -Recurse -Force

Write-Host "[3/3] 打包后端..." -ForegroundColor Cyan
& mvn -f "$root\pom.xml" -DskipTests package

Write-Host "完成:auth-gateway jar 已内含前端;启动 auth-service + auth-gateway 即可访问整站(网关端口)。" -ForegroundColor Green
```

- [ ] **Step 4: 提交**

```bash
git add build-all.ps1 auth-gateway/src/main/resources/static/.gitkeep .gitignore
git commit -m "feat(m9): 一键构建脚本 build-all + 忽略 static 构建产物"
```

---

## Task 7:部署文档 DEPLOY.md

**Files:**
- Create: `docs/DEPLOY.md`

- [ ] **Step 1: 写 DEPLOY.md**

Create `docs/DEPLOY.md`:
```markdown
# 部署手册(auth-wjb)

单端口部署:**网关托管前端 + 后端走 /api**。生产起 auth-service 与 auth-gateway 两个 jar,访问网关端口即整站。

## 1. 前置
- JDK 21
- 可达的 Nacos / MySQL / Redis
- 已在 Nacos 创建配置(见 §2)

## 2. Nacos 配置(生产用 prod 命名空间)
在 Nacos 新建命名空间 `prod`,在其下建以下配置(Group `DEFAULT_GROUP`,YAML):
- `auth-common.yml`:Redis、Sa-Token(同 dev 模板)
- `auth-service-prod.yml`:MySQL、`oauth.github.*`、`auth.dev-return-code: false`(生产关闭验证码回传)
- `auth-gateway-prod.yml`:网关路由
  ```yaml
  spring:
    cloud:
      gateway:
        discovery:
          locator:
            enabled: false
        routes:
          - id: auth-service
            uri: lb://auth-service
            predicates:
              - Path=/api/**
            filters:
              - StripPrefix=1
  ```
> 真实连接/密钥只写在 Nacos,不入 git。

## 3. 构建
仓库根执行:
```
.\build-all.ps1
```
产出:`auth-gateway/target/auth-gateway-0.0.1-SNAPSHOT.jar`(内含前端)、`auth-service/target/auth-service-0.0.1-SNAPSHOT.jar`。

## 4. 启动(生产)
```
java -DNACOS_NAMESPACE=prod -DSPRING_PROFILES_ACTIVE=prod -jar auth-service-0.0.1-SNAPSHOT.jar
java -DNACOS_NAMESPACE=prod -DSPRING_PROFILES_ACTIVE=prod -jar auth-gateway-0.0.1-SNAPSHOT.jar
```
- 默认(不传参)= `test/dev`,本机开发用。
- 访问 `http://<网关host>:8080/` 即整站;后端 API 经 `/api/**`。

## 5. GitHub OAuth 生产回调
在 GitHub OAuth App 的 Authorization callback URL **追加**生产地址:`http://<网关host>:8080/oauth/callback`(保留 dev 的 `http://localhost:5173/oauth/callback`)。

## 6. 附录:Nginx 反代(可选,前后端分离时)
若改用 Nginx 托管前端、网关只做 API:
```nginx
server {
  listen 80;
  location / { root /var/www/auth-wjb; try_files $uri /index.html; }
  location /api/ { proxy_pass http://127.0.0.1:8080; }
}
```
```

- [ ] **Step 2: 提交**

```bash
git add docs/DEPLOY.md
git commit -m "docs(m9): 部署手册 DEPLOY.md"
```

---

## Task 8:本机构建 + 联调验证(单端口)

> 验证生产形态:仅起两个 jar(不依赖 Vite),访问网关 8080。

- [ ] **Step 1: 一键构建**

Run(仓库根):
```powershell
.\build-all.ps1
```
Expected: 末行"完成";网关 jar 内含前端(`auth-gateway/src/main/resources/static/` 有 index.html 与 assets)。

- [ ] **Step 2: 启动两个 jar(默认 test/dev)**

Run(仓库根):
```powershell
$env:JAVA_HOME = "D:\Program Files\Java\jdk-21.0.10"
Start-Process java -ArgumentList "-jar","auth-service\target\auth-service-0.0.1-SNAPSHOT.jar" -RedirectStandardOutput "svc.log" -RedirectStandardError "svc.err.log" -WindowStyle Hidden
Start-Process java -ArgumentList "-jar","auth-gateway\target\auth-gateway-0.0.1-SNAPSHOT.jar" -RedirectStandardOutput "gw.log" -RedirectStandardError "gw.err.log" -WindowStyle Hidden
```
等两个 `Started ...Application`。

- [ ] **Step 3: 脚本验证(单端口 8080)**

Run:
```powershell
function Redis-Get($key){ $c=New-Object Net.Sockets.TcpClient('192.168.0.230',6379);$s=$c.GetStream();$cmd="*2`r`n`$3`r`nGET`r`n`$$($key.Length)`r`n$key`r`n";$b=[Text.Encoding]::ASCII.GetBytes($cmd);$s.Write($b,0,$b.Length);$s.Flush();Start-Sleep -Milliseconds 200;$buf=New-Object byte[] 4096;$n=$s.Read($buf,0,$buf.Length);$r=[Text.Encoding]::ASCII.GetString($buf,0,$n);$c.Close();if($r -match '^\$(-?\d+)\r\n'){$len=[int]$Matches[1];if($len -lt 0){return $null};$idx=$r.IndexOf("`r`n")+2;return $r.Substring($idx,$len)};return $r.Trim() }
"① 首页静态:"; (Invoke-WebRequest "http://localhost:8080/" -UseBasicParsing).Content.Substring(0,60)
"② SPA 回退(刷新 /system/user 应返回 index.html 而非 404):"
"   status=$((Invoke-WebRequest 'http://localhost:8080/system/user' -UseBasicParsing).StatusCode), 含 <div id=app>=$((Invoke-WebRequest 'http://localhost:8080/system/user' -UseBasicParsing).Content.Contains('id="app"'))"
"③ /api 登录(经网关 StripPrefix):"
$cap=Invoke-RestMethod "http://localhost:8080/api/auth/captcha"; $ans=Redis-Get("captcha:"+$cap.data.captchaKey)
$r=Invoke-RestMethod -Method Post "http://localhost:8080/api/auth/login" -ContentType "application/json" -Body (@{username='admin';password='123456';captchaKey=$cap.data.captchaKey;captchaCode=$ans}|ConvertTo-Json)
"   login code=$($r.code) tokenLen=$($r.data.token.Length)"
$h=@{Authorization=$r.data.token}
"④ 带 token 调 /api/system/user/list:"
"   code=$((Invoke-RestMethod 'http://localhost:8080/api/system/user/list' -Headers $h).code)"
"⑤ 无 token 调受保护接口(应 401):"
"   " + ((Invoke-RestMethod 'http://localhost:8080/api/system/user/list' -ErrorAction SilentlyContinue) | ConvertTo-Json -Compress)
```
Expected:① 返回 HTML(含 `<!doctype html`/`<div id="app">`);② status=200 且含 `id="app"`(SPA 回退生效);③ login code=200;④ code=200;⑤ code=401(鉴权剥前缀后仍生效)。

- [ ] **Step 4: 浏览器验证(8080 单端口)**

浏览器开 `http://localhost:8080`:确认登录页正常渲染(静态托管 + 资源加载)。用 playwright 截图确认 UI(登录 + 进一个管理页;captcha 可读图输入或注入 token)。

- [ ] **Step 5: 停服务清理**

Run(仓库根):
```powershell
Get-CimInstance Win32_Process -Filter "Name='java.exe'" | Where-Object { $_.CommandLine -like "*auth-service*" -or $_.CommandLine -like "*auth-gateway*" } | ForEach-Object { try { Stop-Process -Id $_.ProcessId -Force } catch {} }
Remove-Item svc.log,svc.err.log,gw.log,gw.err.log -ErrorAction SilentlyContinue
Remove-Item .playwright-mcp -Recurse -Force -ErrorAction SilentlyContinue
```

---

## 完成标准(M9 验收)

- [ ] `build-all.ps1` 一键构建成功,网关 jar 内含前端。
- [ ] 网关 8080:`/` 出登录页;刷新 `/system/user` 返回 index.html(SPA 回退,不 404)。
- [ ] `/api/auth/login` 经网关登录成功;`/api/system/user/list` 带 token 200、无 token 401(鉴权剥前缀生效)。
- [ ] bootstrap 默认 test/dev;`-DNACOS_NAMESPACE=prod` 可切换。
- [ ] `docs/DEPLOY.md` 完整;`build-all.ps1` 可用;static 构建产物已 gitignore。
- [ ] 所有任务均已 `git commit`。

## 下一里程碑

- **M10/收尾**:`neat-freak` 同步 README/文档(把 DEPLOY、各 M 里程碑、架构图收口),让新人可直接上手。
- 真上服务器 / 容器化:在有目标服务器或 Docker 后,基于 DEPLOY.md 扩展。
```
