# M1:微服务骨架与网关统一鉴权 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 P1 单体重构为 Maven 多模块(common/gateway/service),接入 Nacos 注册与配置中心,在网关用 Sa-Token 做统一登录/权限校验,跑通"经网关访问 /health"与"未登录访问受保护路径被网关拦截"。

**Architecture:** 父 pom 统一管理 Spring Boot/Cloud/Alibaba/Sa-Token 版本。`auth-common` 抽公共类(jar)。`auth-service`(WebMVC,8081)承接 P1 业务骨架并注册 Nacos。`auth-gateway`(WebFlux,8080)为唯一入口,响应式 Sa-Token 过滤器统一鉴权,经 `lb://auth-service` 路由转发。gateway 与 service 共享 Redis 会话。

**Tech Stack:** Spring Boot 3.2.5 + Spring Cloud 2023.0.1 + Spring Cloud Alibaba 2023.0.1.0 + Sa-Token 1.37.0(reactor 版用于网关)+ Nacos + Redis + MySQL。

> 路径基准:仓库根 `auth-wjb/`。包名 `com.wjb.auth`。Maven 命令统一走项目脚本 `backend\build.ps1`(已存在,内部固定 JDK21)——**但本里程碑会删除 `backend/`**,故 Task 1 先把构建脚本迁到仓库根 `build.ps1` 指向新的根 pom。
>
> 中间件:Nacos `192.168.0.230:8848`(账号 `nacos/nacos`)、Redis `192.168.0.230:6379`、MySQL `192.168.0.230:3309`(库 `auth_wjb`,账号见你本地记录)。**敏感连接写入 Nacos 配置,不入 git。**

---

## 现状与目标结构

**现状(P1,单体):**
```
auth-wjb/backend/  (Spring Boot 单体:AuthApplication / common / entity / config / controller)
```

**M1 完成后:**
```
auth-wjb/
├── pom.xml                 # 父 pom(packaging=pom)
├── build.ps1 / build.cmd   # 根级构建脚本(固定 JDK21)
├── auth-common/            # 公共 jar
│   └── src/main/java/com/wjb/auth/common/...
├── auth-service/           # 业务服务(8081)
│   └── src/{main,test}/...
├── auth-gateway/           # 网关(8080)
│   └── src/main/...
├── sql/  docs/             # 不变
```
`backend/` 在 Task 9 删除。

---

## Task 1:父 pom 与根级构建脚本

**Files:**
- Create: `pom.xml`(仓库根)
- Create: `build.ps1`(仓库根)
- Create: `build.cmd`(仓库根)

- [ ] **Step 1: 写父 pom**

Create `pom.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.wjb</groupId>
    <artifactId>auth-wjb</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>auth-wjb</name>

    <modules>
        <module>auth-common</module>
        <module>auth-service</module>
        <module>auth-gateway</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <spring-cloud-alibaba.version>2023.0.1.0</spring-cloud-alibaba.version>
        <sa-token.version>1.37.0</sa-token.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <knife4j.version>4.4.0</knife4j.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring-cloud-alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- 内部模块 -->
            <dependency>
                <groupId>com.wjb</groupId>
                <artifactId>auth-common</artifactId>
                <version>0.0.1-SNAPSHOT</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
```

- [ ] **Step 2: 写根级构建脚本**

Create `build.ps1`:
```powershell
# 根级 Maven 包装脚本:固定 JDK21,作用于整个多模块工程
$env:JAVA_HOME = "D:\Program Files\Java\jdk-21.0.10"
Write-Host "[build.ps1] JAVA_HOME(本次) = $env:JAVA_HOME" -ForegroundColor Cyan
& mvn -f "$PSScriptRoot\pom.xml" @args
```

Create `build.cmd`:
```bat
@echo off
set "JAVA_HOME=D:\Program Files\Java\jdk-21.0.10"
echo [build.cmd] JAVA_HOME(local) = %JAVA_HOME%
mvn -f "%~dp0pom.xml" %*
```

- [ ] **Step 3: 提交(模块目录还没建,先不编译)**

```bash
git add pom.xml build.ps1 build.cmd
git commit -m "feat: 多模块父 pom 与根级构建脚本"
```

---

## Task 2:auth-common 公共模块

**Files:**
- Create: `auth-common/pom.xml`
- Create: `auth-common/src/main/java/com/wjb/auth/common/Result.java`
- Create: `auth-common/src/main/java/com/wjb/auth/common/ResultCode.java`
- Create: `auth-common/src/main/java/com/wjb/auth/common/exception/ServiceException.java`
- Create: `auth-common/src/main/java/com/wjb/auth/common/entity/BaseEntity.java`
- Create: `auth-common/src/main/java/com/wjb/auth/common/constant/SecurityConstants.java`

> 说明:`Result`/`ResultCode` 去掉 P1 里的 `@Schema` 注解,使 auth-common 不依赖 swagger;文档注解保留在各服务的 DTO 上。`BaseEntity` 移到 `common.entity` 包。

- [ ] **Step 1: 写 auth-common/pom.xml**

Create `auth-common/pom.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.wjb</groupId>
        <artifactId>auth-wjb</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>auth-common</artifactId>

    <dependencies>
        <!-- BaseEntity 用到的 MyBatis-Plus 注解(仅注解,轻量) -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-annotation</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
    </dependencies>
</project>
```

- [ ] **Step 2: 写 ResultCode**

Create `auth-common/src/main/java/com/wjb/auth/common/ResultCode.java`:
```java
package com.wjb.auth.common;

/** 统一返回码 */
public enum ResultCode {
    SUCCESS(200, "成功"),
    BAD_REQUEST(400, "请求参数错误"),
    UNAUTHORIZED(401, "登录已过期,请重新登录"),
    FORBIDDEN(403, "无操作权限"),
    ERROR(500, "系统繁忙");

    private final int code;
    private final String msg;

    ResultCode(int code, String msg) {
        this.code = code;
        this.msg = msg;
    }

    public int getCode() { return code; }
    public String getMsg() { return msg; }
}
```

- [ ] **Step 3: 写 Result(无 swagger 注解)**

Create `auth-common/src/main/java/com/wjb/auth/common/Result.java`:
```java
package com.wjb.auth.common;

/** 统一返回结构 */
public class Result<T> {

    private int code;
    private String msg;
    private T data;

    public Result() {}

    public Result(int code, String msg, T data) {
        this.code = code;
        this.msg = msg;
        this.data = data;
    }

    public static <T> Result<T> success(T data) {
        return new Result<>(ResultCode.SUCCESS.getCode(), ResultCode.SUCCESS.getMsg(), data);
    }

    public static <T> Result<T> success() {
        return success(null);
    }

    public static <T> Result<T> fail(int code, String msg) {
        return new Result<>(code, msg, null);
    }

    public static <T> Result<T> fail(ResultCode rc) {
        return new Result<>(rc.getCode(), rc.getMsg(), null);
    }

    public int getCode() { return code; }
    public void setCode(int code) { this.code = code; }
    public String getMsg() { return msg; }
    public void setMsg(String msg) { this.msg = msg; }
    public T getData() { return data; }
    public void setData(T data) { this.data = data; }
}
```

- [ ] **Step 4: 写 ServiceException**

Create `auth-common/src/main/java/com/wjb/auth/common/exception/ServiceException.java`:
```java
package com.wjb.auth.common.exception;

/** 业务异常,携带提示信息 */
public class ServiceException extends RuntimeException {
    public ServiceException(String message) {
        super(message);
    }
}
```

- [ ] **Step 5: 写 BaseEntity(移到 common.entity 包)**

Create `auth-common/src/main/java/com/wjb/auth/common/entity/BaseEntity.java`:
```java
package com.wjb.auth.common.entity;

import com.baomidou.mybatisplus.annotation.FieldFill;
import com.baomidou.mybatisplus.annotation.TableField;
import com.baomidou.mybatisplus.annotation.TableLogic;

import java.io.Serializable;
import java.time.LocalDateTime;

/** 公共审计字段基类,所有实体继承 */
public class BaseEntity implements Serializable {

    @TableField(value = "create_time", fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(value = "update_time", fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;

    @TableField(value = "create_by", fill = FieldFill.INSERT)
    private Long createBy;

    @TableField(value = "update_by", fill = FieldFill.INSERT_UPDATE)
    private Long updateBy;

    @TableLogic
    @TableField("deleted")
    private Integer deleted;

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }
    public LocalDateTime getUpdateTime() { return updateTime; }
    public void setUpdateTime(LocalDateTime updateTime) { this.updateTime = updateTime; }
    public Long getCreateBy() { return createBy; }
    public void setCreateBy(Long createBy) { this.createBy = createBy; }
    public Long getUpdateBy() { return updateBy; }
    public void setUpdateBy(Long updateBy) { this.updateBy = updateBy; }
    public Integer getDeleted() { return deleted; }
    public void setDeleted(Integer deleted) { this.deleted = deleted; }
}
```

- [ ] **Step 6: 写 SecurityConstants(网关与服务约定的请求头名)**

Create `auth-common/src/main/java/com/wjb/auth/common/constant/SecurityConstants.java`:
```java
package com.wjb.auth.common.constant;

/** 安全相关常量:网关与下游服务的契约 */
public final class SecurityConstants {

    private SecurityConstants() {}

    /** 网关鉴权通过后注入、下游读取的当前用户 id 请求头(M2 启用) */
    public static final String HEADER_USER_ID = "X-User-Id";
}
```

- [ ] **Step 7: 编译 auth-common**

Run(仓库根):
```powershell
.\build.ps1 -q -pl auth-common -am compile
```
Expected: BUILD SUCCESS(无输出)。

- [ ] **Step 8: 提交**

```bash
git add auth-common/
git commit -m "feat: auth-common 公共模块(Result/BaseEntity/异常/常量)"
```

---

## Task 3:auth-service 模块 pom

**Files:**
- Create: `auth-service/pom.xml`

- [ ] **Step 1: 写 auth-service/pom.xml**

Create `auth-service/pom.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.wjb</groupId>
        <artifactId>auth-wjb</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>auth-service</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.wjb</groupId>
            <artifactId>auth-common</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>

        <!-- Nacos 注册 + 配置 + bootstrap -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>

        <!-- Sa-Token(M2 登录用,M1 先就位) -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-spring-boot3-starter</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-redis-jackson</artifactId>
            <version>${sa-token.version}</version>
        </dependency>

        <!-- MyBatis-Plus + MySQL -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- 接口文档 -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>${knife4j.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

- [ ] **Step 2: 提交(类还没搬,先不编译)**

```bash
git add auth-service/pom.xml
git commit -m "feat: auth-service 模块 pom"
```

---

## Task 4:auth-service 主类、配置类、健康接口

**Files:**
- Create: `auth-service/src/main/java/com/wjb/auth/AuthApplication.java`
- Create: `auth-service/src/main/java/com/wjb/auth/config/MyBatisPlusConfig.java`
- Create: `auth-service/src/main/java/com/wjb/auth/config/MetaObjectHandlerImpl.java`
- Create: `auth-service/src/main/java/com/wjb/auth/config/Knife4jConfig.java`
- Create: `auth-service/src/main/java/com/wjb/auth/common/GlobalExceptionHandler.java`
- Create: `auth-service/src/main/java/com/wjb/auth/controller/HealthController.java`

> 关键改造:① **不再创建 SaTokenConfig**(MVC 登录拦截器),鉴权上移网关;② `MetaObjectHandlerImpl` 的"当前用户"改为从请求头 `X-User-Id` 读(M1 通常为 null,安全);③ 全局异常处理器保留(含 Sa-Token 异常分支,M2 仍可能在服务内抛)。

- [ ] **Step 1: 写启动类**

Create `auth-service/src/main/java/com/wjb/auth/AuthApplication.java`:
```java
package com.wjb.auth;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.client.discovery.EnableDiscoveryClient;

@SpringBootApplication
@EnableDiscoveryClient
@MapperScan("com.wjb.auth.mapper")
public class AuthApplication {
    public static void main(String[] args) {
        SpringApplication.run(AuthApplication.class, args);
    }
}
```

- [ ] **Step 2: 写 MyBatisPlusConfig**

Create `auth-service/src/main/java/com/wjb/auth/config/MyBatisPlusConfig.java`:
```java
package com.wjb.auth.config;

import com.baomidou.mybatisplus.annotation.DbType;
import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;
import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class MyBatisPlusConfig {

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

- [ ] **Step 3: 写 MetaObjectHandlerImpl(当前用户从请求头读)**

Create `auth-service/src/main/java/com/wjb/auth/config/MetaObjectHandlerImpl.java`:
```java
package com.wjb.auth.config;

import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import com.wjb.auth.common.constant.SecurityConstants;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;
import org.springframework.web.context.request.RequestContextHolder;
import org.springframework.web.context.request.ServletRequestAttributes;

import java.time.LocalDateTime;

/** 插入/更新自动填充审计字段;当前用户从网关注入的 X-User-Id 读取 */
@Component
public class MetaObjectHandlerImpl implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        LocalDateTime now = LocalDateTime.now();
        Long userId = currentUserId();
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, now);
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, now);
        this.strictInsertFill(metaObject, "createBy", Long.class, userId);
        this.strictInsertFill(metaObject, "updateBy", Long.class, userId);
        this.strictInsertFill(metaObject, "deleted", Integer.class, 0);
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
        this.strictUpdateFill(metaObject, "updateBy", Long.class, currentUserId());
    }

    /** 从当前请求头 X-User-Id 取;无则返回 null */
    private Long currentUserId() {
        try {
            ServletRequestAttributes attrs =
                    (ServletRequestAttributes) RequestContextHolder.getRequestAttributes();
            if (attrs != null) {
                String uid = attrs.getRequest().getHeader(SecurityConstants.HEADER_USER_ID);
                if (uid != null && !uid.isBlank()) {
                    return Long.valueOf(uid);
                }
            }
        } catch (Exception ignored) {
        }
        return null;
    }
}
```

- [ ] **Step 4: 写 Knife4jConfig**

Create `auth-service/src/main/java/com/wjb/auth/config/Knife4jConfig.java`:
```java
package com.wjb.auth.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class Knife4jConfig {

    @Bean
    public OpenAPI authOpenAPI() {
        return new OpenAPI().info(new Info()
                .title("auth-service API")
                .description("认证业务服务接口文档")
                .version("v0.0.1"));
    }
}
```

- [ ] **Step 5: 写全局异常处理器**

Create `auth-service/src/main/java/com/wjb/auth/common/GlobalExceptionHandler.java`:
```java
package com.wjb.auth.common;

import com.wjb.auth.common.exception.ServiceException;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/** auth-service 全局异常处理(登录态/权限校验已在网关,这里处理业务/参数/兜底) */
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(ServiceException.class)
    public Result<Void> handleService(ServiceException e) {
        return Result.fail(ResultCode.ERROR.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValid(MethodArgumentNotValidException e) {
        FieldError fe = e.getBindingResult().getFieldError();
        String msg = fe == null ? "参数错误" : fe.getDefaultMessage();
        return Result.fail(ResultCode.BAD_REQUEST.getCode(), msg);
    }

    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        // 实际项目此处应记日志:log.error("系统异常", e);
        return Result.fail(ResultCode.ERROR);
    }
}
```

- [ ] **Step 6: 写健康接口**

Create `auth-service/src/main/java/com/wjb/auth/controller/HealthController.java`:
```java
package com.wjb.auth.controller;

import com.wjb.auth.common.Result;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@Tag(name = "健康检查")
@RestController
public class HealthController {

    @Operation(summary = "服务健康检查")
    @GetMapping("/health")
    public Result<String> health() {
        return Result.success("UP");
    }
}
```

- [ ] **Step 7: 提交(配置文件下个 Task 写,暂不编译)**

```bash
git add auth-service/src/main/java/
git commit -m "feat: auth-service 主类/配置/健康接口(鉴权移除,当前用户改读请求头)"
```

---

## Task 5:auth-service 配置(bootstrap + Nacos + 测试离线配置)

**Files:**
- Create: `auth-service/src/main/resources/bootstrap.yml`
- Create: `auth-service/src/main/resources/application.yml`
- Create: `auth-service/src/test/resources/application.yml`

- [ ] **Step 1: 写 bootstrap.yml(服务名 + Nacos 地址)**

Create `auth-service/src/main/resources/bootstrap.yml`:
```yaml
spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.230:8848
        username: nacos
        password: nacos
      config:
        server-addr: 192.168.0.230:8848
        username: nacos
        password: nacos
        file-extension: yml
        # 共享公共配置(Redis、Sa-Token)
        shared-configs:
          - data-id: auth-common.yml
            refresh: true
```

> Nacos 会按 `${spring.application.name}-${profile}.${file-extension}` = `auth-service-dev.yml` 自动拉取主配置;`shared-configs` 再叠加 `auth-common.yml`。

- [ ] **Step 2: 写 application.yml(本地仅留端口等非敏感项)**

Create `auth-service/src/main/resources/application.yml`:
```yaml
server:
  port: 8081

# 数据库/Redis/Sa-Token 等敏感与环境配置全部来自 Nacos(auth-service-dev.yml + auth-common.yml)
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

knife4j:
  enable: true
```

- [ ] **Step 3: 写测试离线配置(不依赖在线 Nacos)**

Create `auth-service/src/test/resources/application.yml`:
```yaml
server:
  port: 0

spring:
  # 测试关闭 Nacos,避免单测强依赖中间件在线
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
        import-check:
          enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/auth_wjb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: test
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

sa-token:
  token-name: Authorization
  is-log: false
```

> 说明:健康接口测试不访问 DB/Redis(连接为懒加载),故测试用占位 localhost 即可加载上下文,无需真实连接。

- [ ] **Step 4: 编译 auth-service**

Run(仓库根):
```powershell
.\build.ps1 -q -pl auth-service -am compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 5: 提交**

```bash
git add auth-service/src/main/resources/ auth-service/src/test/resources/
git commit -m "feat: auth-service Nacos 配置与测试离线配置"
```

---

## Task 6:auth-service 健康接口测试(TDD,离线可跑)

**Files:**
- Create: `auth-service/src/test/java/com/wjb/auth/HealthControllerTest.java`

- [ ] **Step 1: 写测试**

Create `auth-service/src/test/java/com/wjb/auth/HealthControllerTest.java`:
```java
package com.wjb.auth;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/** 健康接口冒烟测试:验证上下文可离线加载(无 Nacos)、/health 返回统一结构 */
@SpringBootTest
@AutoConfigureMockMvc
class HealthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void health_shouldReturnUp() throws Exception {
        mockMvc.perform(get("/health"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(200))
                .andExpect(jsonPath("$.data").value("UP"));
    }
}
```

- [ ] **Step 2: 运行测试**

Run(仓库根):
```powershell
$out = .\build.ps1 -pl auth-service -am test 2>$null
$out | Select-String -Pattern "Tests run:|BUILD SUCCESS|BUILD FAILURE"
```
Expected: `Tests run: 1, Failures: 0` + `BUILD SUCCESS`。

- [ ] **Step 3: 提交**

```bash
git add auth-service/src/test/java/
git commit -m "test: auth-service 健康接口离线冒烟测试"
```

---

## Task 7:auth-gateway 模块(网关 + Sa-Token 统一鉴权)

**Files:**
- Create: `auth-gateway/pom.xml`
- Create: `auth-gateway/src/main/java/com/wjb/auth/gateway/GatewayApplication.java`
- Create: `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SaTokenGatewayConfig.java`
- Create: `auth-gateway/src/main/resources/bootstrap.yml`
- Create: `auth-gateway/src/main/resources/application.yml`

- [ ] **Step 1: 写 auth-gateway/pom.xml**

Create `auth-gateway/pom.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.wjb</groupId>
        <artifactId>auth-wjb</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>auth-gateway</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.wjb</groupId>
            <artifactId>auth-common</artifactId>
        </dependency>

        <!-- 网关(响应式,自带 WebFlux) -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway</artifactId>
        </dependency>
        <!-- 负载均衡(lb:// 解析) -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <!-- Nacos 注册 + 配置 + bootstrap -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>

        <!-- Sa-Token 响应式 + Redis(读共享会话) -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-reactor-spring-boot3-starter</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-redis-jackson</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

- [ ] **Step 2: 写网关启动类**

Create `auth-gateway/src/main/java/com/wjb/auth/gateway/GatewayApplication.java`:
```java
package com.wjb.auth.gateway;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.client.discovery.EnableDiscoveryClient;

@SpringBootApplication
@EnableDiscoveryClient
public class GatewayApplication {
    public static void main(String[] args) {
        SpringApplication.run(GatewayApplication.class, args);
    }
}
```

- [ ] **Step 3: 写 Sa-Token 网关鉴权配置**

Create `auth-gateway/src/main/java/com/wjb/auth/gateway/config/SaTokenGatewayConfig.java`:
```java
package com.wjb.auth.gateway.config;

import cn.dev33.satoken.context.SaHolder;
import cn.dev33.satoken.exception.NotLoginException;
import cn.dev33.satoken.exception.NotPermissionException;
import cn.dev33.satoken.reactor.filter.SaReactorFilter;
import cn.dev33.satoken.router.SaRouter;
import cn.dev33.satoken.stp.StpUtil;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.HashMap;
import java.util.Map;

/** 网关统一鉴权:校验登录态 + 按路由校验权限码,失败返回统一 JSON */
@Configuration
public class SaTokenGatewayConfig {

    @Bean
    public SaReactorFilter saReactorFilter() {
        return new SaReactorFilter()
                // 拦截全部
                .addInclude("/**")
                // 鉴权逻辑
                .setAuth(obj -> {
                    // 1) 白名单放行(登录、发码、OAuth 回调、文档、健康检查)
                    SaRouter.match("/**")
                            .notMatch(
                                    "/auth/login",
                                    "/auth/login/**",
                                    "/auth/sms-code",
                                    "/auth/email-code",
                                    "/auth/oauth/**",
                                    "/health",
                                    "/doc.html",
                                    "/webjars/**",
                                    "/v3/api-docs/**",
                                    "/swagger-ui/**",
                                    "/favicon.ico"
                            )
                            .check(r -> StpUtil.checkLogin());

                    // 2) 路由 → 权限码(用户管理,M2 业务上线后生效;M1 无 token 时已被上面的 checkLogin 拦下)
                    SaRouter.match("/system/user/**", r -> StpUtil.checkPermission("system:user:list"));
                })
                // 鉴权异常 → 统一 JSON
                .setError(e -> {
                    SaHolder.getResponse().setHeader("Content-Type", "application/json; charset=utf-8");
                    Map<String, Object> body = new HashMap<>();
                    if (e instanceof NotLoginException) {
                        body.put("code", 401);
                        body.put("msg", "登录已过期,请重新登录");
                    } else if (e instanceof NotPermissionException) {
                        body.put("code", 403);
                        body.put("msg", "无操作权限");
                    } else {
                        body.put("code", 500);
                        body.put("msg", e.getMessage());
                    }
                    body.put("data", null);
                    return body; // 直接作为响应体序列化为 {code,msg,data}
                });
    }
}
```

> 说明:`setError` 返回的对象会被序列化为响应体。这里用与 `Result` 一致的 `{code,msg,data}` 结构。HTTP 状态码默认 200,业务码在 body 的 `code` 字段区分(与本项目统一返回风格一致)。

- [ ] **Step 4: 写网关 bootstrap.yml**

Create `auth-gateway/src/main/resources/bootstrap.yml`:
```yaml
spring:
  application:
    name: auth-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.230:8848
        username: nacos
        password: nacos
      config:
        server-addr: 192.168.0.230:8848
        username: nacos
        password: nacos
        file-extension: yml
        shared-configs:
          - data-id: auth-common.yml
            refresh: true
```

- [ ] **Step 5: 写网关 application.yml(端口;路由放 Nacos)**

Create `auth-gateway/src/main/resources/application.yml`:
```yaml
server:
  port: 8080

# 路由规则来自 Nacos 的 auth-gateway-dev.yml;此处仅兜底端口配置
```

- [ ] **Step 6: 编译 auth-gateway**

Run(仓库根):
```powershell
.\build.ps1 -q -pl auth-gateway -am compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 7: 提交**

```bash
git add auth-gateway/
git commit -m "feat: auth-gateway 网关与 Sa-Token 统一鉴权"
```

---

## Task 8:Nacos 配置数据(手动录入 + 全量构建验证)

**Files:**(无代码文件;在 Nacos 控制台创建配置。内容同时存档到仓库便于复现)
- Create: `docs/nacos/auth-common.yml`
- Create: `docs/nacos/auth-service-dev.yml`
- Create: `docs/nacos/auth-gateway-dev.yml`

> 这些是 Nacos 配置的**存档副本**(含真实连接,故 `docs/nacos/` 需加入 .gitignore,不提交)。在 Nacos 控制台(`http://192.168.0.230:8848/nacos`,nacos/nacos)按 Data ID 逐个新建,Group 用 `DEFAULT_GROUP`,格式 `YAML`,命名空间 `public`。

- [ ] **Step 1: 将 nacos 存档目录加入 .gitignore**

在 `.gitignore` 末尾追加:
```gitignore
# Nacos 配置存档(含真实连接,不入库)
docs/nacos/
```

- [ ] **Step 2: 写 auth-common.yml 存档(Redis + Sa-Token,共享)**

Create `docs/nacos/auth-common.yml`:
```yaml
spring:
  data:
    redis:
      host: 192.168.0.230
      port: 6379
      database: 0

sa-token:
  token-name: Authorization
  timeout: 604800
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: false
```

- [ ] **Step 3: 写 auth-service-dev.yml 存档(MySQL)**

Create `docs/nacos/auth-service-dev.yml`:
```yaml
spring:
  datasource:
    url: jdbc:mysql://192.168.0.230:3309/auth_wjb?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: <数据库账号,见本地记录>
    password: "<数据库密码,见本地记录>"
    driver-class-name: com.mysql.cj.jdbc.Driver
```

- [ ] **Step 4: 写 auth-gateway-dev.yml 存档(路由)**

Create `docs/nacos/auth-gateway-dev.yml`:
```yaml
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/auth/**,/system/**,/health
```

- [ ] **Step 5: 在 Nacos 控制台创建以上 3 个配置**

手动操作:浏览器打开 `http://192.168.0.230:8848/nacos`(nacos/nacos)→ 配置管理 → 配置列表 → `+` 新建,逐个录入:
- Data ID `auth-common.yml`,Group `DEFAULT_GROUP`,格式 YAML,内容 = Step 2
- Data ID `auth-service-dev.yml`,同上,内容 = Step 3
- Data ID `auth-gateway-dev.yml`,同上,内容 = Step 4

完成后控制台「配置列表」应能看到这 3 条。

- [ ] **Step 6: 全量构建(三模块一起)**

Run(仓库根):
```powershell
.\build.ps1 -q -DskipTests package
```
Expected: BUILD SUCCESS,生成 `auth-service/target/*.jar` 与 `auth-gateway/target/*.jar`。

- [ ] **Step 7: 提交(仅 .gitignore;docs/nacos 不入库)**

```bash
git add .gitignore
git commit -m "chore: 忽略 Nacos 配置存档目录"
```

---

## Task 9:删除旧单体 backend/ 并联调验证

**Files:**
- Delete: `backend/`(整个旧单体目录)

- [ ] **Step 1: 删除旧 backend 目录**

Run(仓库根):
```bash
git rm -r backend/
```

- [ ] **Step 2: 提交删除**

```bash
git commit -m "refactor: 移除旧单体 backend(已拆分为 common/service/gateway)"
```

- [ ] **Step 3: 启动 auth-service**

Run(仓库根,后台窗口 1):
```powershell
.\build.ps1 -pl auth-service spring-boot:run
```
等待日志出现 `Started AuthApplication`。

- [ ] **Step 4: 启动 auth-gateway**

Run(仓库根,后台窗口 2):
```powershell
.\build.ps1 -pl auth-gateway spring-boot:run
```
等待日志出现 `Started GatewayApplication`。

- [ ] **Step 5: 验证 Nacos 注册**

浏览器打开 `http://192.168.0.230:8848/nacos` → 服务管理 → 服务列表,应看到 **auth-service** 与 **auth-gateway** 两个服务,实例数各 1。

- [ ] **Step 6: 验证经网关访问健康接口**

Run:
```powershell
(Invoke-WebRequest -Uri "http://localhost:8080/health" -UseBasicParsing).Content
```
Expected: `{"code":200,"msg":"成功","data":"UP"}`(请求经网关 8080 → 路由到 auth-service 8081)。

- [ ] **Step 7: 验证未登录访问受保护路径被网关拦截**

Run:
```powershell
try { (Invoke-WebRequest -Uri "http://localhost:8080/system/user/list" -UseBasicParsing).Content } catch { $r=$_.Exception.Response; if($r){ $sr=New-Object IO.StreamReader($r.GetResponseStream()); $sr.ReadToEnd() } else { "ERR" } }
```
Expected: body 含 `"code":401`(网关在转发前直接拦截,请求未进 auth-service)。

- [ ] **Step 8: 停止两个服务**

Run:
```powershell
Get-CimInstance Win32_Process -Filter "Name='java.exe'" | Where-Object { $_.CommandLine -like "*AuthApplication*" -or $_.CommandLine -like "*GatewayApplication*" } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

---

## 完成标准(M1 验收)

- [ ] `.\build.ps1 -q -DskipTests package` 全量构建成功(三模块)。
- [ ] `auth-service` 单测离线通过(`Tests run: 1, Failures: 0`)。
- [ ] Nacos 服务列表可见 `auth-service` 与 `auth-gateway`。
- [ ] `GET http://localhost:8080/health` 经网关返回 `{"code":200,...,"data":"UP"}`。
- [ ] `GET http://localhost:8080/system/user/list` 未登录返回 `code:401`(网关拦截)。
- [ ] 旧 `backend/` 已删除;仓库为多模块结构。
- [ ] 所有任务均已 `git commit`。

## 下一里程碑(M2)

在 `auth-service` 落地 P2 业务(账密登录 + 用户管理 CRUD + StpInterface 权限数据源),并在网关补上 **登录成功后权限快照写入 Redis** 与 **X-User-Id 请求头注入**,打通"登录 → 网关鉴权(含权限码)→ 下游携带用户身份"。原 P2 计划的业务代码可整体复用,落点从单体改为 auth-service 模块。
```
