# P1:后端骨架与公共基础 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭起 Spring Boot 后端骨架与公共基础设施(统一返回、基类实体、全局异常、Sa-Token/MyBatis-Plus/Redis 配置、Knife4j 文档、建表脚本),让项目可启动、可连库、文档页可访问。

**Architecture:** 单仓库 `backend/` 子目录放 Spring Boot 3 应用,单体分包。本里程碑只做地基:不含具体业务接口,但把 `Result`、`BaseEntity`、`MetaObjectHandler`、全局异常处理、Sa-Token 拦截器骨架、配置全部就位,并提供完整 RBAC 建表脚本。完成后用一个健康检查接口 + Knife4j 文档页验证可运行。

**Tech Stack:** Spring Boot 3.x、Sa-Token、MyBatis-Plus、MySQL 8、Redis、springdoc-openapi + Knife4j、JUnit 5。

> 说明:本计划面向"零本仓库上下文"的工程师。所有路径以仓库根 `auth-wjb/` 为基准。Java 包名统一用 `com.wjb.auth`。

---

## 文件结构(本里程碑创建/修改)

```
auth-wjb/
├── .gitignore                                         # 创建
├── README.md                                          # 创建
├── sql/schema.sql                                     # 创建:RBAC 建表脚本
├── backend/
│   ├── pom.xml                                        # 创建:依赖与构建
│   └── src/main/
│       ├── java/com/wjb/auth/
│       │   ├── AuthApplication.java                   # 创建:启动类
│       │   ├── common/Result.java                     # 创建:统一返回
│       │   ├── common/ResultCode.java                 # 创建:返回码枚举
│       │   ├── common/exception/ServiceException.java # 创建:业务异常
│       │   ├── common/exception/GlobalExceptionHandler.java # 创建:全局异常
│       │   ├── entity/BaseEntity.java                 # 创建:公共字段基类
│       │   ├── config/MyBatisPlusConfig.java          # 创建:分页+自动填充
│       │   ├── config/MetaObjectHandlerImpl.java      # 创建:审计字段填充
│       │   ├── config/SaTokenConfig.java              # 创建:拦截器+放行
│       │   ├── config/Knife4jConfig.java              # 创建:接口文档
│       │   └── controller/HealthController.java       # 创建:健康检查
│       └── resources/
│           └── application.yml                        # 创建:配置
└── backend/src/test/java/com/wjb/auth/
    └── HealthControllerTest.java                      # 创建:冒烟测试
```

---

## Task 0:初始化仓库

**Files:**
- Create: `auth-wjb/.gitignore`
- Create: `auth-wjb/README.md`

- [ ] **Step 1: 初始化 git 仓库**

Run(在仓库根 `auth-wjb/`):
```bash
git init
```
Expected: `Initialized empty Git repository ...`

- [ ] **Step 2: 写 .gitignore**

Create `.gitignore`:
```gitignore
# Java
target/
*.class
# IDE
.idea/
*.iml
.vscode/
# Frontend
node_modules/
dist/
# Logs / env
*.log
application-local.yml
```

- [ ] **Step 3: 写 README**

Create `README.md`:
```markdown
# auth-wjb

基于 Sa-Token 的用户权限登录系统(RBAC)。前后端分目录单仓库。

- backend/  Spring Boot 3 + Sa-Token + MyBatis-Plus(端口 8080)
- frontend/ Vue 3 + Element Plus(后续里程碑)
- sql/      建表脚本
- docs/     设计文档与实现计划

## 启动(后端)
1. 建库:`CREATE DATABASE auth_wjb DEFAULT CHARSET utf8mb4;` 并执行 `sql/schema.sql`
2. 启动本机 Redis
3. 改 `backend/src/main/resources/application.yml` 中数据库账号密码
4. `cd backend && mvn spring-boot:run`
5. 文档:http://localhost:8080/doc.html
```

- [ ] **Step 4: 提交**

```bash
git add .gitignore README.md
git commit -m "chore: 初始化仓库与 README"
```

---

## Task 1:Maven 项目骨架与依赖

**Files:**
- Create: `backend/pom.xml`
- Create: `backend/src/main/java/com/wjb/auth/AuthApplication.java`
- Create: `backend/src/main/resources/application.yml`

- [ ] **Step 1: 写 pom.xml**

Create `backend/pom.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.wjb</groupId>
    <artifactId>auth-wjb-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>auth-wjb-backend</name>

    <properties>
        <java.version>17</java.version>
        <sa-token.version>1.37.0</sa-token.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <knife4j.version>4.4.0</knife4j.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Sa-Token 认证 + Redis 集成 -->
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-spring-boot3-starter</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <dependency>
            <groupId>cn.dev33</groupId>
            <artifactId>sa-token-redis-jackson</artifactId>
            <version>${sa-token.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>

        <!-- MyBatis-Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Knife4j(springdoc) -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>${knife4j.version}</version>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

- [ ] **Step 2: 写启动类**

Create `backend/src/main/java/com/wjb/auth/AuthApplication.java`:
```java
package com.wjb.auth;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
@MapperScan("com.wjb.auth.mapper")
public class AuthApplication {
    public static void main(String[] args) {
        SpringApplication.run(AuthApplication.class, args);
    }
}
```

- [ ] **Step 3: 写 application.yml**

Create `backend/src/main/resources/application.yml`:
```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_wjb?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# Sa-Token 配置
sa-token:
  token-name: Authorization
  timeout: 604800          # token 有效期 7 天(秒)
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: false

# Knife4j 增强
knife4j:
  enable: true
```

- [ ] **Step 4: 编译验证骨架可构建**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS(无编译错误)。

- [ ] **Step 5: 提交**

```bash
git add backend/pom.xml backend/src/main/java/com/wjb/auth/AuthApplication.java backend/src/main/resources/application.yml
git commit -m "feat: Spring Boot 后端骨架与依赖"
```

---

## Task 2:统一返回 Result 与返回码

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/common/ResultCode.java`
- Create: `backend/src/main/java/com/wjb/auth/common/Result.java`

- [ ] **Step 1: 写返回码枚举**

Create `backend/src/main/java/com/wjb/auth/common/ResultCode.java`:
```java
package com.wjb.auth.common;

/** 统一返回码 */
public enum ResultCode {
    SUCCESS(200, "成功"),
    BAD_REQUEST(400, "请求参数错误"),
    UNAUTHORIZED(401, "登录已过期,请重新登录"),
    FORBIDDEN(403, "无操作权限"),
    ERROR(500, "系统繁忙");

    private final int code;
    private final String msg;

    ResultCode(int code, String msg) {
        this.code = code;
        this.msg = msg;
    }

    public int getCode() { return code; }
    public String getMsg() { return msg; }
}
```

- [ ] **Step 2: 写 Result**

Create `backend/src/main/java/com/wjb/auth/common/Result.java`:
```java
package com.wjb.auth.common;

import io.swagger.v3.oas.annotations.media.Schema;

/** 统一返回结构 */
@Schema(description = "统一返回结构")
public class Result<T> {

    @Schema(description = "状态码,200 为成功")
    private int code;

    @Schema(description = "提示信息")
    private String msg;

    @Schema(description = "业务数据")
    private T data;

    public Result() {}

    public Result(int code, String msg, T data) {
        this.code = code;
        this.msg = msg;
        this.data = data;
    }

    public static <T> Result<T> success(T data) {
        return new Result<>(ResultCode.SUCCESS.getCode(), ResultCode.SUCCESS.getMsg(), data);
    }

    public static <T> Result<T> success() {
        return success(null);
    }

    public static <T> Result<T> fail(int code, String msg) {
        return new Result<>(code, msg, null);
    }

    public static <T> Result<T> fail(ResultCode rc) {
        return new Result<>(rc.getCode(), rc.getMsg(), null);
    }

    public int getCode() { return code; }
    public void setCode(int code) { this.code = code; }
    public String getMsg() { return msg; }
    public void setMsg(String msg) { this.msg = msg; }
    public T getData() { return data; }
    public void setData(T data) { this.data = data; }
}
```

- [ ] **Step 3: 编译验证**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/common/
git commit -m "feat: 统一返回 Result 与返回码"
```

---

## Task 3:公共实体基类 BaseEntity 与审计字段自动填充

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/entity/BaseEntity.java`
- Create: `backend/src/main/java/com/wjb/auth/config/MetaObjectHandlerImpl.java`
- Create: `backend/src/main/java/com/wjb/auth/config/MyBatisPlusConfig.java`

- [ ] **Step 1: 写 BaseEntity**

Create `backend/src/main/java/com/wjb/auth/entity/BaseEntity.java`:
```java
package com.wjb.auth.entity;

import com.baomidou.mybatisplus.annotation.FieldFill;
import com.baomidou.mybatisplus.annotation.TableField;
import com.baomidou.mybatisplus.annotation.TableLogic;

import java.io.Serializable;
import java.time.LocalDateTime;

/** 公共审计字段基类,所有实体继承 */
public class BaseEntity implements Serializable {

    @TableField(value = "create_time", fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(value = "update_time", fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;

    @TableField(value = "create_by", fill = FieldFill.INSERT)
    private Long createBy;

    @TableField(value = "update_by", fill = FieldFill.INSERT_UPDATE)
    private Long updateBy;

    @TableLogic
    @TableField("deleted")
    private Integer deleted;

    public LocalDateTime getCreateTime() { return createTime; }
    public void setCreateTime(LocalDateTime createTime) { this.createTime = createTime; }
    public LocalDateTime getUpdateTime() { return updateTime; }
    public void setUpdateTime(LocalDateTime updateTime) { this.updateTime = updateTime; }
    public Long getCreateBy() { return createBy; }
    public void setCreateBy(Long createBy) { this.createBy = createBy; }
    public Long getUpdateBy() { return updateBy; }
    public void setUpdateBy(Long updateBy) { this.updateBy = updateBy; }
    public Integer getDeleted() { return deleted; }
    public void setDeleted(Integer deleted) { this.deleted = deleted; }
}
```

- [ ] **Step 2: 写审计字段自动填充处理器**

Create `backend/src/main/java/com/wjb/auth/config/MetaObjectHandlerImpl.java`:
```java
package com.wjb.auth.config;

import cn.dev33.satoken.stp.StpUtil;
import com.baomidou.mybatisplus.core.handlers.MetaObjectHandler;
import org.apache.ibatis.reflection.MetaObject;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;

/** 插入/更新时自动填充审计字段 */
@Component
public class MetaObjectHandlerImpl implements MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        LocalDateTime now = LocalDateTime.now();
        Long userId = currentUserId();
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, now);
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, now);
        this.strictInsertFill(metaObject, "createBy", Long.class, userId);
        this.strictInsertFill(metaObject, "updateBy", Long.class, userId);
        this.strictInsertFill(metaObject, "deleted", Integer.class, 0);
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
        this.strictUpdateFill(metaObject, "updateBy", Long.class, currentUserId());
    }

    /** 未登录(如系统初始化、注册)时返回 null,不抛异常 */
    private Long currentUserId() {
        try {
            if (StpUtil.isLogin()) {
                return StpUtil.getLoginIdAsLong();
            }
        } catch (Exception ignored) {
        }
        return null;
    }
}
```

- [ ] **Step 3: 写 MyBatis-Plus 配置(分页插件)**

Create `backend/src/main/java/com/wjb/auth/config/MyBatisPlusConfig.java`:
```java
package com.wjb.auth.config;

import com.baomidou.mybatisplus.annotation.DbType;
import com.baomidou.mybatisplus.extension.plugins.MybatisPlusInterceptor;
import com.baomidou.mybatisplus.extension.plugins.inner.PaginationInnerInterceptor;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class MyBatisPlusConfig {

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

- [ ] **Step 4: 编译验证**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 5: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/entity/BaseEntity.java backend/src/main/java/com/wjb/auth/config/MetaObjectHandlerImpl.java backend/src/main/java/com/wjb/auth/config/MyBatisPlusConfig.java
git commit -m "feat: BaseEntity 与审计字段自动填充、分页插件"
```

---

## Task 4:全局异常处理

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/common/exception/ServiceException.java`
- Create: `backend/src/main/java/com/wjb/auth/common/exception/GlobalExceptionHandler.java`

- [ ] **Step 1: 写业务异常**

Create `backend/src/main/java/com/wjb/auth/common/exception/ServiceException.java`:
```java
package com.wjb.auth.common.exception;

/** 业务异常,携带提示信息 */
public class ServiceException extends RuntimeException {
    public ServiceException(String message) {
        super(message);
    }
}
```

- [ ] **Step 2: 写全局异常处理器**

Create `backend/src/main/java/com/wjb/auth/common/exception/GlobalExceptionHandler.java`:
```java
package com.wjb.auth.common.exception;

import cn.dev33.satoken.exception.NotLoginException;
import cn.dev33.satoken.exception.NotPermissionException;
import cn.dev33.satoken.exception.NotRoleException;
import com.wjb.auth.common.Result;
import com.wjb.auth.common.ResultCode;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

/** 全局异常处理,统一转 Result */
@RestControllerAdvice
public class GlobalExceptionHandler {

    /** 未登录 / token 失效 */
    @ExceptionHandler(NotLoginException.class)
    public Result<Void> handleNotLogin(NotLoginException e) {
        return Result.fail(ResultCode.UNAUTHORIZED);
    }

    /** 无权限码 */
    @ExceptionHandler(NotPermissionException.class)
    public Result<Void> handleNotPermission(NotPermissionException e) {
        return Result.fail(ResultCode.FORBIDDEN.getCode(), "无操作权限");
    }

    /** 无角色 */
    @ExceptionHandler(NotRoleException.class)
    public Result<Void> handleNotRole(NotRoleException e) {
        return Result.fail(ResultCode.FORBIDDEN.getCode(), "无访问权限");
    }

    /** 业务异常 */
    @ExceptionHandler(ServiceException.class)
    public Result<Void> handleService(ServiceException e) {
        return Result.fail(ResultCode.ERROR.getCode(), e.getMessage());
    }

    /** 参数校验异常 */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValid(MethodArgumentNotValidException e) {
        FieldError fe = e.getBindingResult().getFieldError();
        String msg = fe == null ? "参数错误" : fe.getDefaultMessage();
        return Result.fail(ResultCode.BAD_REQUEST.getCode(), msg);
    }

    /** 兜底 */
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        // 实际项目此处应记录日志:log.error("系统异常", e);
        return Result.fail(ResultCode.ERROR);
    }
}
```

- [ ] **Step 3: 编译验证**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 4: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/common/exception/
git commit -m "feat: 全局异常处理与业务异常"
```

---

## Task 5:Sa-Token 拦截器配置(放行文档与认证接口)

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/config/SaTokenConfig.java`

- [ ] **Step 1: 写 Sa-Token 配置**

Create `backend/src/main/java/com/wjb/auth/config/SaTokenConfig.java`:
```java
package com.wjb.auth.config;

import cn.dev33.satoken.interceptor.SaInterceptor;
import cn.dev33.satoken.stp.StpUtil;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/** Sa-Token 全局登录校验拦截器:除放行路径外,所有接口需登录 */
@Configuration
public class SaTokenConfig implements WebMvcConfigurer {

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(new SaInterceptor(handle -> StpUtil.checkLogin()))
                .addPathPatterns("/**")
                .excludePathPatterns(
                        // 认证接口(后续里程碑实现)
                        "/auth/**",
                        // 健康检查
                        "/health",
                        // Knife4j / springdoc 文档
                        "/doc.html",
                        "/webjars/**",
                        "/v3/api-docs/**",
                        "/swagger-ui/**",
                        "/swagger-resources/**",
                        "/favicon.ico"
                );
    }
}
```

> 说明:这里只做"登录校验";权限码校验(`@SaCheckPermission`)由各业务接口在 P2 用注解声明,无需在此全局配置。

- [ ] **Step 2: 编译验证**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 3: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/config/SaTokenConfig.java
git commit -m "feat: Sa-Token 登录拦截器与放行配置"
```

---

## Task 6:Knife4j 接口文档配置

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/config/Knife4jConfig.java`

- [ ] **Step 1: 写文档配置**

Create `backend/src/main/java/com/wjb/auth/config/Knife4jConfig.java`:
```java
package com.wjb.auth.config;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.security.SecurityScheme;
import io.swagger.v3.oas.models.Components;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class Knife4jConfig {

    @Bean
    public OpenAPI authOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("auth-wjb 用户权限登录系统 API")
                        .description("基于 Sa-Token 的 RBAC 权限系统接口文档")
                        .version("v0.0.1"))
                // 在文档页提供 Authorization header 输入框
                .components(new Components().addSecuritySchemes("Authorization",
                        new SecurityScheme()
                                .type(SecurityScheme.Type.APIKEY)
                                .in(SecurityScheme.In.HEADER)
                                .name("Authorization")));
    }
}
```

- [ ] **Step 2: 编译验证**

Run(在 `backend/`):
```bash
mvn -q compile
```
Expected: BUILD SUCCESS。

- [ ] **Step 3: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/config/Knife4jConfig.java
git commit -m "feat: Knife4j 接口文档配置"
```

---

## Task 7:健康检查接口(TDD)

**Files:**
- Create: `backend/src/main/java/com/wjb/auth/controller/HealthController.java`
- Test: `backend/src/test/java/com/wjb/auth/HealthControllerTest.java`

- [ ] **Step 1: 写失败测试**

Create `backend/src/test/java/com/wjb/auth/HealthControllerTest.java`:
```java
package com.wjb.auth;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/** 健康检查冒烟测试:验证应用上下文可加载、放行路径可匿名访问、Result 结构正确 */
@SpringBootTest
@AutoConfigureMockMvc
class HealthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void health_shouldReturnOk_withoutLogin() throws Exception {
        mockMvc.perform(get("/health"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.code").value(200))
                .andExpect(jsonPath("$.data").value("UP"));
    }
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run(在 `backend/`):
```bash
mvn -q -Dtest=HealthControllerTest test
```
Expected: FAIL —— 编译失败(HealthController 不存在)或 404。

> 前提:测试需能连上本机 MySQL(`auth_wjb` 库)与 Redis;若未就绪,先按 README 启动。若暂时无数据库,可改用 `@SpringBootTest(webEnvironment = ...)` 配 H2,本里程碑默认要求本机 MySQL/Redis 可用。

- [ ] **Step 3: 写健康检查接口**

Create `backend/src/main/java/com/wjb/auth/controller/HealthController.java`:
```java
package com.wjb.auth.controller;

import com.wjb.auth.common.Result;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@Tag(name = "健康检查")
@RestController
public class HealthController {

    @Operation(summary = "服务健康检查")
    @GetMapping("/health")
    public Result<String> health() {
        return Result.success("UP");
    }
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run(在 `backend/`):
```bash
mvn -q -Dtest=HealthControllerTest test
```
Expected: PASS(BUILD SUCCESS,Tests run: 1, Failures: 0)。

- [ ] **Step 5: 手动验证文档页(可选但建议)**

Run(在 `backend/`):
```bash
mvn spring-boot:run
```
浏览器打开 `http://localhost:8080/doc.html`,应看到「auth-wjb 用户权限登录系统 API」与"健康检查"接口。`Ctrl+C` 停止。

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/wjb/auth/controller/HealthController.java backend/src/test/java/com/wjb/auth/HealthControllerTest.java
git commit -m "feat: 健康检查接口与冒烟测试"
```

---

## Task 8:RBAC 建表脚本

**Files:**
- Create: `sql/schema.sql`

- [ ] **Step 1: 写建表脚本**

Create `sql/schema.sql`:
```sql
-- auth-wjb RBAC 建表脚本(MySQL 8, utf8mb4)
-- 执行前:CREATE DATABASE auth_wjb DEFAULT CHARSET utf8mb4;

-- 公共字段约定:create_time / update_time / create_by / update_by / deleted

-- 1. 用户表
CREATE TABLE sys_user (
    id          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
    username    VARCHAR(50)  NOT NULL COMMENT '登录账号',
    password    VARCHAR(100) NULL COMMENT 'BCrypt 密码',
    nickname    VARCHAR(50)  NULL COMMENT '昵称',
    phone       VARCHAR(20)  NULL COMMENT '手机号',
    email       VARCHAR(100) NULL COMMENT '邮箱',
    avatar      VARCHAR(255) NULL COMMENT '头像',
    status      TINYINT      NOT NULL DEFAULT 1 COMMENT '0禁用 1正常',
    dept_id     BIGINT       NULL COMMENT '部门id(预留)',
    create_time DATETIME     NULL,
    update_time DATETIME     NULL,
    create_by   BIGINT       NULL,
    update_by   BIGINT       NULL,
    deleted     TINYINT      NOT NULL DEFAULT 0 COMMENT '0未删 1已删',
    PRIMARY KEY (id),
    UNIQUE KEY uk_username (username),
    UNIQUE KEY uk_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 2. 角色表
CREATE TABLE sys_role (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    role_name   VARCHAR(50)  NOT NULL COMMENT '角色名',
    role_key    VARCHAR(50)  NOT NULL COMMENT '角色标识',
    status      TINYINT      NOT NULL DEFAULT 1,
    remark      VARCHAR(255) NULL,
    create_time DATETIME     NULL,
    update_time DATETIME     NULL,
    create_by   BIGINT       NULL,
    update_by   BIGINT       NULL,
    deleted     TINYINT      NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE KEY uk_role_key (role_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 3. 菜单/权限表
CREATE TABLE sys_menu (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    parent_id   BIGINT       NOT NULL DEFAULT 0 COMMENT '父id,0顶级',
    menu_name   VARCHAR(50)  NOT NULL COMMENT '菜单名',
    menu_type   CHAR(1)      NOT NULL COMMENT 'M目录 C菜单 F按钮',
    path        VARCHAR(200) NULL COMMENT '前端路由',
    component   VARCHAR(255) NULL COMMENT '前端组件',
    perm        VARCHAR(100) NULL COMMENT '权限码',
    icon        VARCHAR(100) NULL,
    sort        INT          NOT NULL DEFAULT 0,
    visible     TINYINT      NOT NULL DEFAULT 1,
    create_time DATETIME     NULL,
    update_time DATETIME     NULL,
    create_by   BIGINT       NULL,
    update_by   BIGINT       NULL,
    deleted     TINYINT      NOT NULL DEFAULT 0,
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='菜单权限表';

-- 4. 用户-角色关联
CREATE TABLE sys_user_role (
    id          BIGINT   NOT NULL AUTO_INCREMENT,
    user_id     BIGINT   NOT NULL,
    role_id     BIGINT   NOT NULL,
    create_time DATETIME NULL,
    update_time DATETIME NULL,
    create_by   BIGINT   NULL,
    update_by   BIGINT   NULL,
    deleted     TINYINT  NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_role (user_id, role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户角色关联';

-- 5. 角色-菜单关联
CREATE TABLE sys_role_menu (
    id          BIGINT   NOT NULL AUTO_INCREMENT,
    role_id     BIGINT   NOT NULL,
    menu_id     BIGINT   NOT NULL,
    create_time DATETIME NULL,
    update_time DATETIME NULL,
    create_by   BIGINT   NULL,
    update_by   BIGINT   NULL,
    deleted     TINYINT  NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE KEY uk_role_menu (role_id, menu_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色菜单关联';

-- 6. 第三方账号绑定
CREATE TABLE sys_oauth_binding (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    user_id     BIGINT       NOT NULL,
    provider    VARCHAR(20)  NOT NULL COMMENT 'wechat/github',
    open_id     VARCHAR(100) NOT NULL,
    union_id    VARCHAR(100) NULL,
    create_time DATETIME     NULL,
    update_time DATETIME     NULL,
    create_by   BIGINT       NULL,
    update_by   BIGINT       NULL,
    deleted     TINYINT      NOT NULL DEFAULT 0,
    PRIMARY KEY (id),
    UNIQUE KEY uk_provider_openid (provider, open_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='第三方账号绑定';

-- 初始数据:超级管理员(密码 123456 的 BCrypt,P2 真正校验前可先用)
-- 密码占位:登录功能在 P2 实现时由代码生成 BCrypt 写入,此处仅建账号骨架
INSERT INTO sys_user (id, username, nickname, status, deleted)
VALUES (1, 'admin', '超级管理员', 1, 0);
INSERT INTO sys_role (id, role_name, role_key, status, deleted)
VALUES (1, '超级管理员', 'admin', 1, 0);
INSERT INTO sys_user_role (user_id, role_id, deleted) VALUES (1, 1, 0);
```

- [ ] **Step 2: 执行建表脚本验证可运行**

Run:
```bash
mysql -uroot -proot -e "CREATE DATABASE IF NOT EXISTS auth_wjb DEFAULT CHARSET utf8mb4;"
mysql -uroot -proot auth_wjb < sql/schema.sql
```
Expected: 无报错;`SHOW TABLES;` 可见 6 张 `sys_*` 表。

- [ ] **Step 3: 提交**

```bash
git add sql/schema.sql
git commit -m "feat: RBAC 建表脚本与初始数据"
```

---

## 完成标准(P1 验收)

- [ ] `cd backend && mvn -q compile` 成功。
- [ ] `mvn -q -Dtest=HealthControllerTest test` 通过(需本机 MySQL `auth_wjb` 库 + Redis)。
- [ ] `mvn spring-boot:run` 启动后,`http://localhost:8080/doc.html` 文档页可打开,显示「健康检查」接口。
- [ ] `GET /health` 匿名可访问,返回 `{"code":200,"msg":"成功","data":"UP"}`。
- [ ] `sql/schema.sql` 在 MySQL 8 执行成功,6 张表就位。
- [ ] 所有任务均已 `git commit`。

## 下一里程碑

P1 验收通过后,编写 **P2:RBAC 用户/角色/菜单 CRUD + 账密登录 + Sa-Token 权限注解**。届时基于本里程碑的 `Result` / `BaseEntity` / 异常处理 / Sa-Token 配置直接扩展。
